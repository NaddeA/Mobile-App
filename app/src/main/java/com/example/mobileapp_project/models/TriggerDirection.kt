package com.example.mobileapp_project.models

enum class TriggerDirection {
    BOTH, ABOVE, BELOW
}

enum class AlarmType {
    TYPE1, TYPE2, TYPE3 // Adjust these names based on actual alarm types in your app.
}
